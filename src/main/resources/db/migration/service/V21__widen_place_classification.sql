-- lcls1 의 폭을 30 에서 100 으로 넓힙니다.
--
-- 고캠핑의 induty 칸에 부대시설 목록이 통째로 들어간 행이 23 건 있습니다.
--   "일반야영장,침대,TV,에어컨,냉장고,유무선인터넷,난방기구,취사도구,내부화장실,..."
-- 가장 긴 값이 79 자입니다.
--
-- 소스 쪽 입력 오류이며 우리가 고칠 수 없습니다.
-- 그래도 잘라 담지 않는 이유는 이 컬럼의 존재 이유가 "가공하지 않고 그대로 담는 것" 이기 때문입니다.
-- 원천 분류를 남겨 두어야 place_type 매핑 규칙을 나중에 바꿔도 다시 뽑을 수 있습니다.
--
-- 자를 규칙을 만들 수도 없습니다.
-- 쉼표 앞까지만 담으면 "일반야영장,글램핑" 같은 정상 다중값도 잘립니다.
-- 정상 값과 오염을 가르는 기준이 데이터에 없습니다.
--
-- place_type 판정에는 영향이 없습니다.
-- 고캠핑은 소스만 보고 CAMPING 으로 정하며 induty 를 읽지 않습니다.
-- 걸리는 것은 원문을 담는 이 컬럼의 폭 하나뿐입니다.
--
-- 100 인 이유는 실측 최장이 79 자이고 소스 오염은 더 길어질 수 있기 때문입니다.
-- source_id 를 200 으로 잡은 것과 같은 판단입니다.

ALTER TABLE place ALTER COLUMN lcls1 TYPE varchar(100);

-- lcls2 와 lcls3 은 varchar(30) 을 그대로 둡니다.
-- 공사가 주는 분류 코드(VE030500 형태)만 들어와 8 자를 넘지 않습니다.
-- 고캠핑과 문화정보원은 이 두 컬럼을 채우지 않습니다.
